import { HighlighttextserachingDirective } from './highlighttextseraching.directive';

describe('HighlighttextserachingDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighttextserachingDirective();
    expect(directive).toBeTruthy();
  });
});
